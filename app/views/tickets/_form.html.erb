<%= form_with(model: ticket) do |form| %>
  <% if ticket.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ticket.errors.count, "error") %> prohibited this ticket from being saved:</h2>

      <ul>
        <% ticket.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :event_id, style: "display: block" %>
    <%= form.text_field :event_id %>
  </div>

  <div>
    <%= form.label :attendee_email, style: "display: block" %>
    <%= form.text_field :attendee_email %>
  </div>
  <br>
  <div>
    <%= form.submit "Create Ticket",style: "padding: 10px 15px; border-radius: 5px; background-color: deepskyblue; color: black"%>
  </div>
<% end %>
